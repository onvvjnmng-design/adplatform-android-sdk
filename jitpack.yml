jdk:
  - openjdk17
before_install:
  - wget -q https://services.gradle.org/distributions/gradle-8.2-bin.zip -O /tmp/gradle.zip
  - unzip -q /tmp/gradle.zip -d $HOME
install:
  - $HOME/gradle-8.2/bin/gradle :ad-sdk:assembleRelease :ad-sdk:publishToMavenLocal

